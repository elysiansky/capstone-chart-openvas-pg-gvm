apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-chart-openvas-pg-gvm.fullname" . }}
  labels:
    {{- include "capstone-chart-openvas-pg-gvm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-chart-openvas-pg-gvm.selectorLabels" . | nindent 4 }}
